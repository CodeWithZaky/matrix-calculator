import React from 'react'

const TitleMatrix = (props) => {

  return (
    <div className='hidden sm:block sm:text-3xl md:text-5xl font-bold font-mono text-center'>{props.title}</div>
  )
}

export default TitleMatrix