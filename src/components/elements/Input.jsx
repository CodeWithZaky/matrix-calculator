const Input = ({ defaultValue }) => {
  return (
    <input
      defaultValue={defaultValue}
      className="PROCESS__INPUT w-[50px] rounded-lg border border-black bg-slate-50 text-center text-xl font-semibold text-slate-900"
    />
  );
};

export default Input;
