const Input = ({ defaultValue }) => {
  return (
    <input
      defaultValue={defaultValue}
      className="bg-slate-50 border border-black rounded-lg w-[50px] font-semibold text-center text-slate-900 text-xl PROCESS__INPUT"
    />
  );
};

export default Input;
