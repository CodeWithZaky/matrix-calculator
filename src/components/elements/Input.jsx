const Input = ({ defaultValue }) => {
  return (
    <input
      defaultValue={defaultValue}
      className="PROCESS__INPUT border border-black w-[50px] rounded-lg text-center text-xl bg-slate-50 font-semibold"
    />
  );
};

export default Input;
